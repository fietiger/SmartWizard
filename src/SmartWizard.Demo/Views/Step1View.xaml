<UserControl x:Class="SmartWizard.Demo.Views.Step1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="20">
        <TextBlock Text="请填写您的基本信息" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
        
        <Label Content="姓名:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,15" 
                 Padding="8,5"/>
        
        <Label Content="邮箱:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,15" 
                 Padding="8,5"/>
        
        <TextBlock Text="请确保信息准确无误，这将用于后续的配置。" 
                   FontSize="12" 
                   Foreground="Gray" 
                   TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>